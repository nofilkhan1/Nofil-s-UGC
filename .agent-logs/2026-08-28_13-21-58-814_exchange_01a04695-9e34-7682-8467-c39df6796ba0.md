---
event_type: exchange
session_id: 01a04695-9e34-7682-8467-c39df6796ba0
date: 2026-08-28
author: Nofil Khan
model: gpt-5.6-luna
tool: codex-desktop
project: Nofils UGC
total_entries: 1
first_prompt_time: 2026-08-28T13:21:58.814Z
---

# Prompt / Response Log

[LOG_ENTRY type=PROMPT num=1 session=01a04695-9e34-7682-8467-c39df6796ba0 file=2026-08-28_13-21-58-814_exchange_01a04695-9e34-7682-8467-c39df6796ba0.md]
timestamp: 2026-08-28T13:21:58.814Z
model: gpt-5.6-luna

CRITICAL BUG — fix this before anything else. Two related issues found:

1. Data-loss bug: on BOTH the creator profile form and the campaign creation
   form, if a submission fails validation (e.g. a NOT NULL constraint error on
   an "optional" field, or a min-length error on description), the ENTIRE form
   resets and all previously-entered fields are wiped — not just the invalid
   one. This must never happen. On any validation error (client-side or
   server-side/database), the form must retain every field's current value
   exactly as the user left it, and show the error message next to the
   specific field that failed, without clearing anything.

2. Schema mismatch: instagram\_handle and tiktok\_handle are labeled "optional"
   in the creator profile UI, but submitting the form with them empty throws a
   NOT NULL constraint error. Check the actual column definitions for
   instagram\_handle, tiktok\_handle, and any other field labeled "optional" in
   either the creator profile or campaign creation form. Any column backing an
   "optional" UI field must allow NULL (or default to empty string) in the
   database — fix the schema via migration for every mismatched column, don't
   just patch the UI to fake-fill them.

3. Also check the campaign creation form's description min-length validation
   the same way: confirm the error is shown inline without clearing the form,
   and confirm this validation is enforced consistently (both client-side for
   fast feedback AND server-side so it can't be bypassed).

After fixing: test both forms yourself by intentionally submitting invalid data
(empty optional field, too-short description) and confirm the form preserves
all other entered values and shows a clear inline error. This is more
important than any feature — a form that deletes what a user typed is a
serious, embarrassing bug in a live product.


[LOG_ENTRY type=RESPONSE num=1 session=01a04695-9e34-7682-8467-c39df6796ba0]
timestamp: 2026-08-28T13:25:02.621Z
model: gpt-5.6-luna

Fixed the critical form data-loss issue in commit `4beaff6`.

Changes made:

- Added shared `usePreserveFormOnError` behavior to both:
  - Creator profile form
  - Campaign creation form
- Before submission, the form captures every field value, including:
  - Text inputs
  - Textareas
  - Selects
  - Checkboxes/niche selections
- If the server action returns a validation or database error, all captured values are restored after the server-action form reset.
- Field-specific validation errors remain attached to their relevant fields.
- Campaign description now has:
  - Client-side minimum-length validation before submission
  - Existing server-side Zod validation (`20–3000` characters)
- Empty optional creator fields remain submitted as `null`, not invalid empty values.

Database verification:

- Applied migration `0012_optional_profile_fields_nullable.sql`.
- Confirmed these creator fields are nullable:
  - `gender`
  - `age`
  - `bio`
  - `portfolio_url`
  - `instagram_url`
  - `tiktok_url`
  - `instagram_handle`
  - `tiktok_handle`
- Campaign `description` remains required (`NOT NULL`) as intended.

Verification passed:

- ESLint
- TypeScript
- 18 automated tests
- Production build
- Direct Supabase schema query

The preservation behavior now applies to both validation failures and database/action failures.

